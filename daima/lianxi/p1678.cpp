#include<bits/stdc++.h>
uisng namespace std;
int m,n,a[100009],b[100009];
int main(){
    cin>>m>>n;
    for(int i=0;i<m;i++) cin>>a[i];
    for(int i=0;i<n;i++) cin>>b[i];
    sort(a,a+m);
    sort(b,b+n);
    int ans=0;
    for(int i=0;i<n;i++){
        if(ef(b[i])){
            
        }
    }
    return 0;
}